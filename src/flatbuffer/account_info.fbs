include "common.fbs";

namespace AccountInfo;

table AccountInfo {
  pubkey: string;
  lamports: uint64;
  owner: string;
  executable: bool;
  rent_epoch: uint64;
  data: [uint8];
  write_version: uint64;
  slot: uint64;
  is_startup: bool;
}

root_type AccountInfo;
